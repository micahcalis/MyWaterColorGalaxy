#pragma once

#include "System/Components/UI/UITransform.hpp"

namespace Beer::Rendering
{
    class QuadBufferUpload
    {
    private:
        size_t size;
        std::vector<System::UITransform> quadTransforms;

    public:
    };
} // namespace Beer::Rendering
